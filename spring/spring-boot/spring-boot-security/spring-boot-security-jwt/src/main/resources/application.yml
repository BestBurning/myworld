server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security?useSSL=false&characterEncoding=utf8&useUnicode=true&useAffectedRows=true&allowPublicKeyRetrieval=true
    username: root
    password: admin
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#  security:
#    user:
#      name: user
#      password: admin
